{
  "name": "picobase-decap",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'build'",
    "picobase-build": "rm -rf dist && npm run build && npm run wrangler-dry-build && cp picobase.json dist/ && cp -r public dist/public && npm run make-pbapp",
    "wrangler-dry-build": "wrangler deploy --dry-run --bundle --outdir dist",
    "make-pbapp": "cd dist && zip -r ./app.pbapp . && mv ./app.pbapp ../ && cd .. && rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "wrangler": "^4.14.1"
  },
  "dependencies": {
    "decap-cms": "^3.6.2",
    "yaml": "^2.7.1"
  }
}
